---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Explanatory posts on space and orbital physics.' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <title>{title}</title>
  </head>
  <body class="starfield">
    <div class="mx-auto flex min-h-screen max-w-3xl flex-col px-5">
      <header class="flex items-center justify-between py-6">
        <a href={import.meta.env.BASE_URL} class="group flex items-center gap-2 text-lg font-bold tracking-tight">
          <span
            class="inline-block h-3 w-3 rounded-full bg-accent-500 shadow-[0_0_10px_2px_rgba(59,130,246,0.5)] transition group-hover:bg-accent-400"
          ></span>
          <span>Orbital Notes</span>
        </a>
        <nav class="flex gap-5 text-sm text-ink-400">
          <a class="transition hover:text-ink-50" href={import.meta.env.BASE_URL}>Home</a>
          <a class="transition hover:text-ink-50" href={`${import.meta.env.BASE_URL}about`}>About</a>
        </nav>
      </header>

      <main class="flex-1 py-6">
        <slot />
      </main>

      <footer class="border-t border-surface-800 py-8 text-sm text-ink-400">
        <p>
          Orbital Notes — a friendly tour of how things fly through space.
          Built with Astro, React islands & KaTeX.
        </p>
      </footer>
    </div>
  </body>
</html>
